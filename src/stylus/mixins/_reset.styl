reset(type = null)

  if type == 'list'
    padding: 0
    margin: 0
    list-style: none

  else if type == 'button' || type == 'btn' || type == 'select'
    appearance: none
    border: none
    background: none

  else
    padding: 0
    margin: 0
